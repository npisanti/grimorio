#!/bin/bash

if [ -z "$1" ]; then
    exec /home/$USER/oF/addons/np-miscela/miscela/bin/miscela
else
    FILEPATH=$(realpath "$1")
    exec /home/$USER/oF/addons/np-miscela/miscela/bin/miscela $FILEPATH
fi

exit
