#!/bin/bash

MULTIARGS=""

for var in "$@"
do
    FILEPATH=$(realpath "$var")    
    MULTIARGS="$MULTIARGS$FILEPATH "
done

exec /home/$USER/oF/apps/np-miscela/miscela/bin/miscela $MULTIARGS
#launch /home/$USER/oF/apps/np-miscela/miscela/bin/miscela $MULTIARGS

exit
