#!/bin/bash

MULTIARGS=""

for var in "$@"
do
    FILEPATH=$(realpath "$var")    
    MULTIARGS="$MULTIARGS$FILEPATH "
done

exec /home/$USER/oF/addons/np-miscela/miscela/bin/miscela $MULTIARGS

exit
