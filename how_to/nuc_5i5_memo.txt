

sudo nano /etc/modprobe.d/iwlwifi.conf
//add
options iwlwifi 11n_disable=1

download iwlwifi-7265D-12.ucode from wireless.kernel.org 
and copy to /lib/firmware/

//todo:
check “fast tsc calibration failed”


also add to autostart 
sudo modprobe snd_seq_midi

//to automatic add at startup
sudo nano /etc/modules-load.d/modules.conf
//add line
snd_seq_midi

//install xorg video drivers from backport
apt-get -t jessie-backports install xserver-xorg-video-intel
